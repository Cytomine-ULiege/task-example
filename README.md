# Task integration example

This repository is an example for integrating an algorithm packaged as a Cytomine task.

## Algorithm

The algorithm generates a sequence of numbers of a specified size and computes their sum.
